;; You may define helper functions here
;; helper function to get edges 
(defun get_edge (graph node)
    (cdr ())
)
(defun find-sequence (g1 g2 start target k)
    ;; TODO: Incomplete function
    ;; The next line should not be in your solution.
    (list 'incomplete)
)
